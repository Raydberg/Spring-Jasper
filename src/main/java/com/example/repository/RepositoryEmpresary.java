package com.example.repository;

import com.example.entity.Empresary;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryEmpresary extends JpaRepository<Empresary,Long> {
}
